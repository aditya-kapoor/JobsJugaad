<h3>Here are the short listed candidates for the Job</h3>
<table>
  <th>Name</th>
  <% @job_applications.each do |application| %>
    <tr>
      <td><%= application.job_seeker.name %></td>
      <td>
        <%= link_to "Call for interview", calling_for_interview_path(:seeker_id=>application.job_seeker_id,:job_id=>application.job_id) %>
      </td>
    </tr>
  <% end %>
</table>
<%= link_to "View Those Who are called for interview", view_called_for_interview_path(:job_id => params[:job_id]) %>